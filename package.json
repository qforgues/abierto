{
  "name": "abierto",
  "version": "1.1.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm --prefix frontend run build",
    "start": "node backend/app.js",
    "qr": "node scripts/generate-qr.js",
    "qr:verify": "node scripts/verify-qr.js"
  },
  "devDependencies": {
    "jsqr": "^1.4.0",
    "pngjs": "^7.0.0",
    "qrcode": "^1.5.4"
  }
}
