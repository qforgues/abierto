services:
  - type: web
    name: abierto
    runtime: node
    buildCommand: cd backend && npm install && cd ../frontend && npm install && npm run build
    startCommand: node backend/app.js
    # Liveness only — deliberately NOT /api/health/ready. A Turso blip must not make Render
    # cycle the container; restarting the app cannot fix someone else's database.
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://abierto.app
      - key: DB_PATH
        value: /var/data/abierto.db
      - key: CORS_ORIGIN
        sync: false
      - key: JWT_SECRET
        sync: false
    plan: free
    domains:
      - abierto.app
      - www.abierto.app
